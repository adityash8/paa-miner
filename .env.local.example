# Optional residential proxy pool, comma-separated
# Example: https://user:pass@proxy1.example.com:8080,https://user:pass@proxy2.example.com:8080
PROXY_POOL=

# Force mobile or desktop UA override (optional: mobile | desktop)
DEFAULT_DEVICE=mobile

# Hard caps to avoid bans / runaway runs
MAX_NODES=220
MAX_RUNTIME_MS=45000
